{
  "name": "{{chance.name()}}",
  "friends": [ "{{_$config}}", null, {
    "name": "{{chance.name()}}",
    "payment_method": {
      "type": "{{chance.cc_type()}}",
      "card": "{{chance.cc()}}",
      "expiration": "{{chance.exp()}}"
    }
  }]
}
