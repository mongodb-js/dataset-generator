{
  "name": "{{chance.name()}}",
  "friends": [ "{{_$config}}", null, {
    "name": "{{chance.name()}}",
    "payment_method": [ "{{_$config}}", null, {
      "type": "{{chance.cc_type()}}",
      "card": "{{chance.cc()}}",
      "expiration": "{{chance.exp()}}"
    }]
  }]
}
