{
  "name": "{{chance.name()}}",
  "friends": [{
    "name": "{{chance.name()}}",
    "payment_method": [{
      "type": "{{chance.cc_type()}}",
      "card": "{{chance.cc()}}",
      "expiration": "{{chance.exp()}}"
    }]
  }]
}
