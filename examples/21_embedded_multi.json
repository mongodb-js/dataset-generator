{
  "user_email": "{{chance.email()}}",
  "job": {
    "company": "{{chance.word()}}",
    "phone": "{{chance.phone()}}",
    "duties": "{{chance.sentence()}}"
  },
  "payment_method": {
    "type": "{{chance.cc_type()}}",
    "card": "{{chance.cc()}}",
    "expiration": "{{chance.exp()}}"
  }
}
