{
  "user_email": "{{chance.email()}}",
  "job": {
    "company": "{{chance.word()}}",
    "phone": "{{chance.phone()}}",
    "duties": "{{chance.sentence()}}"
  },
  "personalities": {
    "favorites": {
      "number": "{{chance.d10()}}",
      "city": "{{chance.city()}}",
      "radio": "{{chance.radio()}}"
    },
    "rating": "{{chance.d6()}}"
  },
  "payment_method": {
    "type": "{{chance.cc_type()}}",
    "card": "{{chance.cc()}}",
    "expiration": "{{chance.exp()}}"
  }
}
