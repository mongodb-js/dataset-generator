{
  "user_email": "{{chance.email()}}",
  "job": {
    "company": "{{chance.word()}}",
    "phones": {
      "mobile": "{{chance.phone()}}",
      "work": "{{chance.phone()}}"
    },
    "duties": "{{chance.sentence()}}"
  },
  "personalities": {
    "favorites": {
      "number": "{{chance.d10()}}",
      "city": "{{chance.city()}}",
      "radio": "{{chance.radio()}}"
    },
    "violence-rating": "{{chance.d6()}}"
  },
  "friends" : [{
    "name": "{{chance.name()}}",
    "phones": ["{{chance.phone()}}"]
  }]
}
