{
  "name": "{{chance.name()}}",
  "address": {
    "street": "{{chance.address()}}",
    "city": "{{chance.city()}}",
    "state": "{{chance.state()}}",
    "zip": "{{chance.zip({plusfour: true})}}"
  }
}
